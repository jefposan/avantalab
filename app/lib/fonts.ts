import { Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-avantalab-inter',
  fallback: ['Arial', 'Helvetica', 'sans-serif'],
});

'use client'; import { useState } from 'react'; import { departments, roles } from '../data/mockData'; import { DataTable, PageIntro, Panel, PlaceholderModal, PrimaryButton, SecondaryButton, StatusBadge } from './RhUi';
export default function DepartmentsPage(){const [modal,setModal]=useState('');return <><PageIntro eyebrow="Estrutura" title="Departamentos e cargos" description="Organize áreas, lideranças e funções da empresa." action={<div className="flex gap-2"><SecondaryButton onClick={()=>setModal('Novo cargo')}>+ Novo cargo</SecondaryButton><PrimaryButton onClick={()=>setModal('Novo departamento')}>+ Novo departamento</PrimaryButton></div>}/><div className="grid gap-6 xl:grid-cols-2"><Panel title="Departamentos" subtitle="6 áreas ativas"><DataTable headers={['Departamento','Responsável','Pessoas','Centro de custo','Status']} rows={departments.map(d=>[...d.slice(0,4),<StatusBadge key="s" value={d[4]}/>])}/></Panel><Panel title="Cargos" subtitle="Funções cadastradas"><DataTable headers={['Cargo','Departamento','Pessoas','Descrição','Status']} rows={roles.map(r=>[...r.slice(0,4),<StatusBadge key="s" value={r[4]}/>])}/></Panel></div><PlaceholderModal open={!!modal} title={modal} onClose={()=>setModal('')}/></>}

'use client'; import { useState } from 'react'; import EmployeeTable from './EmployeeTable'; import NewEmployeeWizard from './NewEmployeeWizard'; import { Filters, PageIntro, Panel, PrimaryButton } from './RhUi';
export default function CollaboratorsPage(){const [open,setOpen]=useState(false);return <><PageIntro eyebrow="Equipe" title="Todos os colaboradores" description="Consulte dados básicos, vínculo e situação de cada pessoa." action={<PrimaryButton onClick={()=>setOpen(true)}>+ Novo colaborador</PrimaryButton>}/><Filters search="Buscar por nome, email ou telefone" selects={['Todos os departamentos','Todos os cargos','Todos os status']}/><Panel title="Lista de colaboradores" subtitle="42 registros • exibição em tabela"><EmployeeTable/></Panel><NewEmployeeWizard open={open} onClose={()=>setOpen(false)}/></>}

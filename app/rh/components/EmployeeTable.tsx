import { employees } from '../data/mockData'; import { DataTable, StatusBadge } from './RhUi';
export default function EmployeeTable() { return <DataTable headers={['Colaborador','Cargo','Departamento','Admissão','Contrato','Status','Ações']} rows={employees.map(e => [<div key={e.id} className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#e5f0f5] text-xs font-semibold text-[#07355d]">{e.initials}</span><div><p className="font-medium text-slate-800">{e.name}</p><p className="text-xs text-slate-400">{e.email}</p></div></div>,e.role,e.department,e.admission,e.contract,<StatusBadge key="s" value={e.status}/>,<span key="a" className="text-slate-400">•••</span>])} linkColumn={{index:0, href: row => `/rh/colaboradores/${employees.find(e => (row[0] as React.ReactElement).key === e.id)?.id || employees[0].id}`}}/>; }
